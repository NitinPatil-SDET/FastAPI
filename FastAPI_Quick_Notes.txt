FASTAPI QUICK NOTES 🚀
=================================

📌 What is FastAPI?
---------------------------------
FastAPI is a modern Python framework used to build APIs quickly.

Benefits:
✅ Fast performance
✅ Automatic Swagger Docs
✅ Data validation
✅ Easy to learn
✅ Type hints support


🏗️ Basic Structure
---------------------------------

    Client
      |
      v
  FastAPI App
      |
      v
   Endpoint
      |
      v
   Response (JSON)


🚀 Create FastAPI App
---------------------------------
from fastapi import FastAPI

app = FastAPI()


🌐 First API
---------------------------------
@app.get("/")
def home():
    return {"message": "Hello World"}


📥 Path Parameters
---------------------------------
URL:

/users/101

Example:

from fastapi import Path

@app.get("/users/{user_id}")
def get_user(
    user_id: int = Path(..., ge=1, description="User ID")
):
    return {"user_id": user_id}


🎯 Why Use Path()?
---------------------------------

           Path()
              |
    -------------------
    |        |        |
Validation Docs   Constraints

✅ Validate input
✅ Add descriptions
✅ Min/Max rules
✅ Better Swagger docs


📤 Query Parameters
---------------------------------

/products?name=laptop

@app.get("/products")
def get_products(name: str):
    return {"name": name}


📦 Request Body
---------------------------------
from pydantic import BaseModel

class User(BaseModel):
    name: str
    age: int

@app.post("/users")
def create_user(user: User):
    return user


🔄 HTTP Methods
---------------------------------
GET     -> Read Data
POST    -> Create Data
PUT     -> Update Data
DELETE  -> Delete Data


📚 Swagger Documentation
---------------------------------
Run server:

uvicorn main:app --reload

Open:
http://127.0.0.1:8000/docs


Status code 
------------------
200 - correct respose
201 - User create
204 - not cotent

400 - Bas Request
401 - Unauthorised
403 - Forbidden
404 - Not found

500 - Internal server
502 - Bad gateway
503 - server unavlaible 


notes on HTTPExpection in python 

Query parameter :
sortby (name, age, weight, height)
order desc/asce (we can put it as option)
/patient?city=Delhi&sort_by=age
? ke baad start quiry parameter
cach paratameter is key-value pair
---------------------------------------
Paydentic 
use for data validation
fastapi, yaml, data science and ml pipeline
WHY - 
type hunting
data validation as (negative age)
step1 : build pynatic class and define ideal schema of the data

Step2: instaniate the model with raw input data (useally a dict or json like stucure)
    automatily validate data and correct into python type

Step3: Pass the validate model object to fuction or use it through your codebase
    clean, type-safe and logically valid data 

How to set required field, optional field, default field.
to verify email - EmailStr
to verify URL - AnyUrl
    